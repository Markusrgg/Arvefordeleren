@page "/will-selection"
@using Arvefordeleren_ClassLibrary.Models

<NavLink class="btn btn-primary" href="/individual-will">Videre til Individuelt Testamente</NavLink>
<NavLink class="btn btn-primary" href="/joint-will">Videre til Fælles Testamente</NavLink>
@* @inject NavigationManager Navigation *@


@* @code {
    private Testator testator = new Testator
        {
            WillType = WillType.Individual // Standardværdi
        };

    private void NavigateToIndividualWill()
    {
        Navigation.NavigateTo("/individual-will");
    }

    private void NavigateToJointWill()
    {
        Navigation.NavigateTo("/joint-will");
    }
} *@

